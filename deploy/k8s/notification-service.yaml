apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: payment-switch
spec:
  ports:
    - port: 9090
      targetPort: 9090
      name: grpc
    - port: 9095
      targetPort: 9095
      name: metrics
  selector:
    app: notification-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: payment-switch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: payment-switch/notification-service:latest
          ports:
            - containerPort: 9090
            - containerPort: 9095
          envFrom:
            - configMapRef:
                name: payment-switch-config
          readinessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 9090
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 64Mi
